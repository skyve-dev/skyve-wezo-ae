{
  "name": "wezo-monorepo",
  "version": "1.0.0",
  "description": "A monorepo for the Wezo project",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev -w server",
    "build:server": "npm run build -w server",
    "test:server": "npm run test -w server",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}